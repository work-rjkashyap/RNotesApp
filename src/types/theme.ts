export interface Theme {
  background: string;
  text: string;
  primary: string;
  secondary: string;
  accent: string;
  error: string;
  surface: string;
  card: string;
  cardAccent: string;
  border: string;
  shadow: string;
}
